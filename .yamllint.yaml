---
extends: default

rules:
  # Matches the Python line length in pyproject.toml.
  line-length:
    max: 120
  # The tool's own config files are documentation as much as configuration, so
  # comment-heavy blocks are expected.
  comments:
    min-spaces-from-content: 1
  truthy:
    check-keys: false
