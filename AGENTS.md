# Conventions

## Imports

Follow the Google Python style guide.

- Import modules, not the names inside them.*
- Always the full package path.
- `as` only when the style guide allows it
  — two modules with the same name
  - a name too generic to leave bare in its context
- The stdlib is exempt.
